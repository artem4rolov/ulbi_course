import {getCounter, getCounterValue} from './selectors'
import {counterActions, counterReducer, counterSlice} from './slice/conter-slice'
import {CounterSchema} from './types/conter-slice.types'

export {counterActions, counterReducer, counterSlice, CounterSchema, getCounter, getCounterValue}